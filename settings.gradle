pluginManagement {
	repositories {
		mavenLocal()
		gradlePluginPortal()
		maven {
			name = 'NeoForge'
			url = "${neo_maven}"
		}
	}
}

plugins {
	id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}